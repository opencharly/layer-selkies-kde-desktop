# 2026.243.1848 — chore(deps): advance pod-kde-selkies and pod-selkies-core to current

## Summary

Both candy pins in this manifest are stale against tags produced **earlier in this same repair
campaign**:

```
-            - '@github.com/opencharly/pod-selkies-core:v2026.243.1829'
+            - '@github.com/opencharly/pod-selkies-core:v2026.243.1844'
-            - '@github.com/opencharly/pod-kde-selkies:v2026.243.0831'
+            - '@github.com/opencharly/pod-kde-selkies:v2026.243.1836'
```

A published tag embeds the pins it carried **at mint time**, so a closure reaching those candies
*through this repo* inherits what their old tags carried. Here that is a day-235 `layer-cuda`,
four hops down:

```
layer-cuda:v2026.235.2121
    <- layer-supervisord@v2026.239.1300
    <- pod-dbus@v2026.239.1555
    <- pod-kde-selkies@v2026.243.0831
    <- THIS REPO
    <- <consuming tree>
```

`pod-dbus` and `pod-kde-selkies` were both already repaired at their sources. This repo still
pointed at the tags from *before* those repairs, which is what kept the conflict alive — fixing a
candy re-tags it, which makes every consumer's pin of it stale in turn.

## How tested

**1. The diff.**

```
$ git diff --stat origin/main...HEAD
 charly.yml | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)
```

**2. Every pin in this manifest, checked against its owning repo's newest tag** — a complete
alignment, not a partial one:

```
  ok     pod-kde-selkies            v2026.243.1836
  ok     pod-selkies-core           v2026.243.1844
```

2 pins, 0 stale on the committed tree. Produced by resolving each ref's owning repo with
`git ls-remote --tags --refs` and comparing to the pinned tag. That distinction matters: advancing
pins piecemeal in a stale tree multiplies skew — measured earlier in this campaign, a partial
advance took a consuming closure from 2 conflicts to 26 before coming back down.

**3. The conflict this clears, measured in a consuming closure** with a stock `task build:binary`
charly (`distro-cachyos`, every in-tree ref at its newest tag):

```
$ charly box validate
warning: candy github.com/opencharly/layer-cuda resolved to multiple versions;
  using newest … , ignoring … (from layer-cuda@v2026.235.2121)
charly box validate: OK — 2 warnings, 0 errors
```

The chain above is the trace from that closure's transitive ref graph back to this repo. That
closure stood at **12** such conflicts when this campaign began and **2** now; this PR targets one
of the two remaining.

## Why it cannot be fixed downstream

The consuming project cannot clear it: the stale pins live inside **this** repo's published tag,
not in theirs. `charly box reconcile` there rewrites only that project's own pins and reports no
change against this warning, so the remedy the workflow rule prescribes cannot reach it.

## The candy's own `version:` is deliberately NOT bumped

`PickCandyVersion` (`sdk/loaderkit/candy_version.go`) warns only when two materializations of a
bare ref carry **different per-entity `version:` values**; identical entity versions across
different git tags resolve silently on tag freshness. Bumping the stamp for a pin-only change
would manufacture the exact warning this PR removes, for every consumer still pinning an older tag
of this repo.

## Rulebook compliance

- **R1 (RCA)** — the conflict was traced through the closure's ref graph to this link and the
  mechanism verified against the resolver source, not inferred from a text search.
- **R3 (no duplication)** — pins only.
- **R4 (no workarounds)** — advanced at the tag that introduces the staleness.
- **R5 (delete legacy)** — the stale pins are replaced.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7** — the pin audit and the consuming-closure measurement are pasted above rather than
  described.

## Change classification

- **Change class:** dependency pins — two `@github` refs. No packages, services, plan steps, or
  entity-version change.
- **Verification gate:** `git diff --stat` on the committed tree, a full pin audit against live
  tags showing zero remaining staleness, and a `charly box validate` measurement in a consuming
  closure — all pasted above.
- **Attribution tier:** `analysed on a live system` — the pin audit was executed live against each
  owning repo and the conflict this clears was measured live in a consuming closure with a stock
  charly build, with both outputs pasted. Short of the runtime tier because a pin-only change
  ships no runtime path of its own to execute and this repo carries no bed.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*