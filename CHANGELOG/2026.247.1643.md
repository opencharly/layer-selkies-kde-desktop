# 2026.247.1643 — chore: align pod-selkies-core pin to v2026.247.1638

## Summary

Aligns this repo's two stale pins — pod-selkies-core v2026.243.1844 → v2026.247.1638 and pod-kde-selkies v2026.243.1836 → v2026.247.1546 — to their merged newest tags. The stale pins' content resolves pod-selkies at v2026.243.1737 (the dual-pin warning chain across the selkies graph that blocks the charly cachyos-import re-vendor). Opencharly umbrella pin-hygiene workstream; recorded in plan/crabbox-loop-cutover.md.

## How tested

```
charly box validate   # repo layer-selkies-kde-desktop, this head
charly box validate: OK — checked 59 candies, 7 distros, 7 builders; 0 warnings, 0 errors
```

The pasted validate output is the graph resolution of this head's composition: it fetched pod-selkies-core:v2026.247.1638 and pod-kde-selkies:v2026.247.1546 and resolved at 0 warnings / 0 errors — the stale-tag warning source is gone at this repo. The full downstream graph gate (the cachyos import) re-runs at the distro re-vendor PR once all coordinated pin alignments land.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the pin change. |
| R1 — RCA every anomaly | Dual-pin warning traced to this repo's two stale pins (their content resolves pod-selkies at v2026.243.1737); fixed at the source. |
| R3 — no duplication | One canonical newest version per candy. |
| R4 — no workarounds | Pin to the merged newest tags; no suppressions. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | box validate 0/0 pasted (real run on the committed tree). |
| R10 — fresh disposable proof | Resolution validated at 0/0 on the committed tree. |

## Change classification

Dependency pin alignment (config-only, 2 lines). **Attribution:** analysed on a live system — the confidence value for this change class (repo composition resolved at 0/0 on a live run of the fresh binary; the full-gate task verify / graph-wide re-vendor is the coordinated downstream PR, not run here for this config-only change). Footer confidence: high confidence.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*